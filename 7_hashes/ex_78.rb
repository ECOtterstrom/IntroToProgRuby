  # 7_hashes exercise 8
  
  # What problem causes the following error?
  
  # NoMethodError: undefined method `keys' for Array
  
  # B. There is no method called keys for Array objects.
  # (We have invoked the keys method on an array, and there is no method called keys for array objects, only for hashes.)